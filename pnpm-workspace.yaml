packages:
  - "apps/*"

# One instance of each WASM-backed package, so the app and the library see the
# same keyhive.
overrides:
  "@automerge/automerge-repo": 2.6.0-subduction.48
  "@automerge/automerge-subduction": 0.16.1

onlyBuiltDependencies:
  - esbuild
